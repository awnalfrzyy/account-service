package strigops.account;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@SpringBootTest
@ActiveProfiles("test")
class AccountApplicationTests {

	@Test
	void contextLoads() {
		// Basic context loading test to ensure the application starts up correctly
		// This verifies that all beans are properly configured and dependencies are resolved
	}

}
